<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.FaceRecognition">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/face_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.FaceRecognition.LoginActivity"
            android:label="@string/title_activity_login"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.FaceRecognition.ForgetPasswordActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.example.FaceRecognition.RegisterActivity"
            android:launchMode="singleTask" />
        <activity android:name="com.example.FaceRecognition.PwdLoginActivity" />
        <activity
            android:name="com.example.FaceRecognition.ResetPassword"
            android:launchMode="singleTask" />
        <activity android:name="com.example.FaceRecognition.PersonalActivity" />
        <activity android:name="com.example.FaceRecognition.FaceIdentify"
            android:launchMode="standard"
            />
    </application>

</manifest>